[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "a3dl-backend"
version = "0.1.0"
description = "Autonomous Adversarial Attack and Defense Lab backend"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"
